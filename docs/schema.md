# Data Model

Core entities:

- Author
- Series
- Genre
- Book
- ReadingSession
- Discussion
- ReaderProfile
- ReaderDna
- Hypothesis
- Prediction

Important idea: `Book` is the object, `ReadingSession` is the experience, `Discussion` is interpretation, `Prediction` is model output.
